server {
    listen {{ server_port }} ;
    server_name  {{ server_name }} ;

    location / {
        root {{ filepath }};
        index index.php index.html index.htm index.nginx-debian.html;
        # autoindex on;
    }
}